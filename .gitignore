# OS
.DS_Store
Thumbs.db

# Editors
.idea/
.vscode/
*.swp

# Claude Code local settings
.claude/settings.local.json

# Secrets and local config
.env
.env.*
!.env.example
*.pem
credentials.json

# Node
node_modules/
dist/
build/
.next/
coverage/
*.log
.pnpm-store/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/

# Local data
*.sqlite
*.sqlite3
*.db

# Real household data lives here and never gets committed. The directory's
# contents are ignored rather than the directory itself, because git will not
# re-include a file under an ignored directory. The example file is the one
# exception and it holds nothing but placeholders.
/data/*
!/data/family.example.json

# Next.js
.next/
next-env.d.ts
tsconfig.tsbuildinfo
